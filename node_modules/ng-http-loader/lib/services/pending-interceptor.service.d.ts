import { HttpEvent, HttpHandler, HttpInterceptor, HttpRequest } from '@angular/common/http';
import { ExistingProvider } from '@angular/core';
import { Observable } from 'rxjs';
export declare class PendingInterceptorService implements HttpInterceptor {
    private _pendingRequests;
    private _pendingRequestsStatus;
    private _filteredUrlPatterns;
    private _forceByPass;
    /** @deprecated Deprecated in favor of pendingRequestsStatus$ */
    readonly pendingRequestsStatus: Observable<boolean>;
    readonly pendingRequestsStatus$: Observable<boolean>;
    readonly pendingRequests: number;
    readonly filteredUrlPatterns: RegExp[];
    forceByPass: boolean;
    private shouldBypassUrl(url);
    intercept(req: HttpRequest<any>, next: HttpHandler): Observable<HttpEvent<any>>;
}
export declare const PendingInterceptorServiceInterceptor: ExistingProvider[];
